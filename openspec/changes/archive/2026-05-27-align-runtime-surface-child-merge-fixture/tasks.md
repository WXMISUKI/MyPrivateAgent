## 1. Spec And Diagnosis

- [x] 1.1 Reproduce the failing Runtime Surface governance run-state test and identify the child merge semantic drift root cause.
- [x] 1.2 Create OpenSpec proposal, design, and delta specs for fixture/contract alignment.
- [x] 1.3 Validate the active OpenSpec change before implementation.

## 2. Implementation

- [x] 2.1 Align the Runtime Surface governance run-state fixture with explicit child executor binding opt-in prerequisites.
- [x] 2.2 Verify the previously failing Runtime Surface test now passes.
- [x] 2.3 Verify missing opt-in remains blocked in existing SDK guard coverage.

## 3. Documentation And Verification

- [x] 3.1 Update runtime contract docs and roadmap with the fixture/prerequisite alignment.
- [x] 3.2 Run focused backend tests and OpenSpec validation.
- [x] 3.3 Archive the completed OpenSpec change and commit the slice.
