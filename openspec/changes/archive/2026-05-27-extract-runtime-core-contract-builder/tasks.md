## 1. Spec And Design

- [x] 1.1 Create the OpenSpec proposal, design, and delta spec for the Runtime Core builder extraction.
- [x] 1.2 Validate the active OpenSpec change before implementation.

## 2. Implementation

- [x] 2.1 Add a dedicated `RuntimeCoreContractBuilder` for default shell and scoped overlay assembly.
- [x] 2.2 Refactor `RuntimeSurfaceService._build_runtime_core_contract()` to delegate to the builder while preserving compatibility.
- [x] 2.3 Add focused backend tests for default Runtime Core, scoped overlay, child merge evidence, and wrapper delegation.

## 3. Documentation And Verification

- [x] 3.1 Update runtime contract architecture docs and roadmap status.
- [x] 3.2 Run focused backend tests and OpenSpec validation.
- [x] 3.3 Archive the completed OpenSpec change and commit the slice.
