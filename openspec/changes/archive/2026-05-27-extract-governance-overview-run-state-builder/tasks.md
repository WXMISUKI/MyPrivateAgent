## 1. Spec And Design

- [x] 1.1 Create the OpenSpec proposal, design, and delta specs for the governance run-state builder extraction.
- [x] 1.2 Validate the active OpenSpec change before implementation.

## 2. TDD And Implementation

- [x] 2.1 Add focused tests for `GovernanceOverviewRunStateBuilder` and verify they fail before the builder exists.
- [x] 2.2 Add the dedicated run-state builder and delegate `governance_overview.run` assembly through it.
- [x] 2.3 Verify builder tests and the Runtime Surface governance run-state integration test pass.

## 3. Documentation And Verification

- [x] 3.1 Update canonical OpenSpec specs, runtime contract docs, and roadmap.
- [x] 3.2 Run focused backend tests and OpenSpec validation.
- [x] 3.3 Archive the completed OpenSpec change and commit the slice.
