## 1. Spec Alignment

- [x] 1.1 Align `runtime-core-terms-model` wording with the canonical `query / run / child_run / approval / trace / audit` definitions.
- [x] 1.2 Align `query-run-read-model` wording so read model boundaries explicitly reference the Runtime Core terminology.
- [x] 1.3 Align `governance-view-unification` wording so both governance views share the same `query / run` interpretation.

## 2. Docs and Verification

- [x] 2.1 Update `docs/architecture/runtime_contracts.md` and related docs to reflect the final term alignment.
- [x] 2.2 Run focused OpenSpec validation for the touched specs and the new change.
- [x] 2.3 Verify the change does not introduce new runtime contract drift.
