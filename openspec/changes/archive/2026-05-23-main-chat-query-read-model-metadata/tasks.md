# Task List

- [x] Add self-describing metadata to `main_chat_query_detail` and `main_chat_query_history` in the runtime surface builder.
- [x] Extend focused runtime surface tests to assert the new metadata without changing existing detail/history behavior.
- [x] Update contract snapshot guards and docs so the metadata becomes part of the runtime truth source.
- [x] Validate the change with OpenSpec and targeted backend tests.
