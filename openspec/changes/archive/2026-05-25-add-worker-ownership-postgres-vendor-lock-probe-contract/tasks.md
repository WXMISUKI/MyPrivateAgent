## 1. Contract

- [x] 1.1 Add PostgreSQL advisory lock probe contract builder and embed it into the vendor lock adapter contract.
- [x] 1.2 Expose PostgreSQL probe evidence from the production gate vendor lock section.

## 2. Gates And Tests

- [x] 2.1 Add focused worker ownership tests for default blocked probe, ready descriptive probe, adapter embedding, and production gate evidence.
- [x] 2.2 Extend runtime smoke, Quality Gate, and Runtime Contract Gate checks for PostgreSQL probe evidence.

## 3. Docs And Validation

- [x] 3.1 Update runtime contract and roadmap docs plus canonical OpenSpec specs.
- [x] 3.2 Run focused backend verification and OpenSpec validation.
- [x] 3.3 Archive the completed OpenSpec change after specs and tasks are complete.
