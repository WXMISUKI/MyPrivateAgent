# Sample Customer Intake Workflow

This is a non-production sample that demonstrates how to migrate a Coze workflow into a governed workflow asset.

It intentionally stays in `draft` status and has no executable handler. It is safe for authoring, registry, and smoke validation work because it does not change default chat behavior.
